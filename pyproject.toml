[project]
name = "one-to-many"
version = "0.1.0"
description = "One-to-Many RNN Lab: Fantasy Name Generator"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0",
    "numpy>=1.24",
    "matplotlib>=3.7",
    "tqdm>=4.65",
]
